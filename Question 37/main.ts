function make_shirt(size: string = "medium" , message: string = "I love python") {
    console.log(`Making a ${size} t-shirt with the message "${message}" printed on it.`);
}

make_shirt("small", "100 days of Coding Challenge");
make_shirt(); 
make_shirt("large",);
